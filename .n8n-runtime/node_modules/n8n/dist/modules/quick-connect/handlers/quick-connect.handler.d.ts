import type { ICredentialDataDecryptedObject } from 'n8n-workflow';
import type { QuickConnectOption } from '../quick-connect.config';
export interface IQuickConnectHandler {
    readonly credentialType: string;
    getCredentialData?(config: QuickConnectOption): Promise<ICredentialDataDecryptedObject>;
}
export declare class QuickConnectHandlerRegistry {
    private readonly handlers;
    register(handler: IQuickConnectHandler): void;
    get(credentialType: string): IQuickConnectHandler | undefined;
}
